pre-commit:
  parallel: true
  stage_fixed: true
  commands:
    lint:
      run: golangci-lint run ./...
    fmt:
      glob: "*.go"
      run: gofumpt -l -w {staged_files}
    swag:
      run: make swag

pre-push:
  commands:
    test:
      run: make test
