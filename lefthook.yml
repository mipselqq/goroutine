pre-commit:
  parallel: true
  stage_fixed: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run {staged_files}
    fmt:
      glob: "*.go"
      run: gofumpt -l -w {staged_files}
    swag:
      run: make swag

pre-push:
  commands:
    test:
      run: make test
